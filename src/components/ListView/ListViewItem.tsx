const ListViewItem = () => {
  return (
    <li>
      <button className="w-full whitespace-nowrap overflow-hidden text-ellipsis p-4 outline-none">
        Example Task Title That is a bit long Example Task Title That is a bit
        long Example Task Title That is a bit long Example Task Title That is a
        bit long
      </button>
    </li>
  );
};

export default ListViewItem;
